package com.dreamscale.ideaflow.core.feeds.story.see;

import com.dreamscale.ideaflow.core.feeds.story.StoryFrame;

public interface IdeaFlowObserver {

    void see(StoryFrame storyFrame, Window window);
}
